"""
🐍 Введение в Pandas - ваш новый суперинструмент!
Этот скрипт демонстрирует основные возможности Pandas
"""

import pandas as pd
import numpy as np
from datetime import datetime

print("🎯 Добро пожаловать в мир Pandas!")
print("=" * 40)

# 1. Создание простой Series
print("\n📊 Создаем Series (умный список):")
продажи = pd.Series([100, 150, 200, 175], 
                   index=['Пн', 'Вт', 'Ср', 'Чт'],
                   name='Продажи')

print(продажи)
print(f"Продажи в среду: {продажи['Ср']}")
print(f"Общие продажи: {продажи.sum()}")
print(f"Средние продажи: {продажи.mean():.2f}")

# 2. Создание DataFrame (умная таблица)
print("\n📋 Создаем DataFrame (умная таблица):")
команда = pd.DataFrame({
    'Имя': ['Иван', 'Мария', 'Петр'],
    'Продажи': [100000, 150000, 120000],
    'Регион': ['Москва', 'СПб', 'Казань'],
    'Опыт_лет': [3, 5, 2]
})

print(команда)

# 3. Базовая аналитика
print("\n🔍 Базовый анализ данных:")
print(f"Размер команды: {len(команда)} человек")
print(f"Общие продажи: {команда['Продажи'].sum():,}")
print(f"Лучший продавец: {команда.loc[команда['Продажи'].idxmax(), 'Имя']}")
print(f"Средний опыт: {команда['Опыт_лет'].mean():.1f} лет")

# 4. Группировка
print("\n📊 Группировка по регионам:")
by_region = команда.groupby('Регион')['Продажи'].sum()
print(by_region)

print("\n✅ Основы Pandas освоены! Переходите к практическим заданиям.")
