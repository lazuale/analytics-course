# 📊 Excel Template: Доверительные интервалы и статистические тесты

## 🎯 Описание шаблона

Этот документ содержит готовые формулы Excel для расчета доверительных интервалов и проведения статистических тестов. Шаблон разработан для главы 6 курса "Аналитика данных" и содержит все необходимые расчеты.

---

## 📈 Лист 1: Доверительный интервал для среднего

### Исходные данные (столбец A)
```
A1: Данные
A2: =число1
A3: =число2
...
A101: =число100
```

### Расчетная таблица (столбцы D-E)

| Ячейка | Название показателя | Формула | Пример |
|--------|-------------------|---------|--------|
| D3 | Размер выборки (n) | `=СЧЁТЗ(A:A)` | 100 |
| D4 | Среднее значение (x̄) | `=СРЗНАЧ(A:A)` | 2540.50 |
| D5 | Стандартное отклонение (s) | `=СТАНДОТКЛОН.В(A:A)` | 480.20 |
| D6 | Стандартная ошибка (SE) | `=D5/КОРЕНЬ(D3)` | 48.02 |
| | | | |
| D8 | Уровень доверия | `0,95` | 95% |
| D9 | Альфа (α) | `=1-D8` | 0.05 |
| D10 | Степени свободы (df) | `=D3-1` | 99 |
| D11 | t-критическое | `=СТЬЮДРАСПОБР(D9;D10)` | 1.984 |
| | | | |
| D13 | Погрешность (E) | `=D11*D6` | 95.28 |
| D14 | Нижняя граница ДИ | `=D4-D13` | 2445.22 |
| D15 | Верхняя граница ДИ | `=D4+D13` | 2635.78 |
| D16 | Ширина интервала | `=D15-D14` | 190.56 |

### Формула "одной строкой" (альтернативная)
```excel
D18: =ДОВЕРИТ(D9;D5;D3)  # Функция ДОВЕРИТ возвращает погрешность
```

### Интерпретация (ячейка D20)
```excel
="Средний чек составляет " & ОКРУГЛ(D4;2) & "₽, с " & ОКРУГЛ(D8*100;0) & "% вероятностью истинный средний чек лежит в диапазоне [" & ОКРУГЛ(D14;2) & "₽; " & ОКРУГЛ(D15;2) & "₽]"
```

---

## 📊 Лист 2: Доверительный интервал для пропорции

### Исходные данные
- Столбец A: данные с значениями 1 (успех) или 0 (неудача)

### Расчетная таблица

| Ячейка | Показатель | Формула | Пример |
|--------|------------|---------|--------|
| D3 | Размер выборки (n) | `=СЧЁТЗ(A:A)` | 10000 |
| D4 | Количество успехов (x) | `=СУММ(A:A)` | 570 |
| D5 | Выборочная пропорция (p) | `=D4/D3` | 0.057 |
| D6 | Пропорция в % | `=D5*100` | 5.7% |
| | | | |
| D8 | Стандартная ошибка (SE) | `=КОРЕНЬ(D5*(1-D5)/D3)` | 0.00232 |
| D9 | z-критическое (95%) | `1,96` | 1.96 |
| D10 | Погрешность | `=D9*D8` | 0.00455 |
| | | | |
| D12 | Нижняя граница ДИ | `=D5-D10` | 0.05245 |
| D13 | Верхняя граница ДИ | `=D5+D10` | 0.06155 |
| D14 | Нижняя граница % | `=D12*100` | 5.245% |
| D15 | Верхняя граница % | `=D13*100` | 6.155% |

### Проверка условий применимости
```excel
D17: =ЕСЛИ(И(D4>=5; D3-D4>=5); "Условия выполнены"; "Условия НЕ выполнены")
```

### Интерпретация
```excel
D19: ="Конверсия составляет " & ОКРУГЛ(D6;2) & "%, 95% ДИ: [" & ОКРУГЛ(D14;2) & "%; " & ОКРУГЛ(D15;2) & "%]"
```

---

## 🧪 Лист 3: Двухвыборочный t-тест

### Исходные данные
- Столбец A: данные группы 1
- Столбец B: данные группы 2

### Описательная статистика

| Ячейка | Показатель | Группа A | Группа B |
|--------|------------|----------|----------|
| D3 | Размер выборки | `=СЧЁТЗ(A:A)` | `=СЧЁТЗ(B:B)` |
| D4 | Среднее | `=СРЗНАЧ(A:A)` | `=СРЗНАЧ(B:B)` |
| D5 | Стандартное отклонение | `=СТАНДОТКЛОН.В(A:A)` | `=СТАНДОТКЛОН.В(B:B)` |
| D6 | Стандартная ошибка | `=D5/КОРЕНЬ(D3)` | `=E5/КОРЕНЬ(E3)` |

### t-тест

| Ячейка | Показатель | Формула |
|--------|------------|---------|
| D9 | Разность средних | `=D4-E4` |
| D10 | Объединенная дисперсия | `=((D3-1)*D5^2+(E3-1)*E5^2)/(D3+E3-2)` |
| D11 | SE разности | `=КОРЕНЬ(D10*(1/D3+1/E3))` |
| D12 | t-статистика | `=D9/D11` |
| D13 | Степени свободы | `=D3+E3-2` |
| D14 | p-value (двухсторонний) | `=ТТЕСТ(A:A;B:B;2;2)` |
| D15 | p-value (односторонний) | `=ТТЕСТ(A:A;B:B;1;2)` |

### Альтернативный расчет t-статистики
```excel
D17: =ABS((D4-E4)/КОРЕНЬ((D5^2/D3)+(E5^2/E3)))  # Формула Уэлча
```

### Доверительный интервал для разности средних
```excel
D19: =СТЬЮДРАСПОБР(0,05;D13)  # t-критическое
D20: =D9-D19*D11  # Нижняя граница
D21: =D9+D19*D11  # Верхняя граница
```

### Интерпретация результата
```excel
D23: =ЕСЛИ(D14<0.05;"Различие статистически значимо";"Различие статистически НЕ значимо")
D24: ="p-value = " & ОКРУГЛ(D14;4) & " при α = 0.05"
```

---

## 📏 Лист 4: Одновыборочный t-тест

### Тест против известного значения μ₀

| Ячейка | Показатель | Формула |
|--------|------------|---------|
| D3 | Тестируемое значение (μ₀) | `3500` |
| D4 | Размер выборки | `=СЧЁТЗ(A:A)` |
| D5 | Выборочное среднее | `=СРЗНАЧ(A:A)` |
| D6 | Стандартное отклонение | `=СТАНДОТКЛОН.В(A:A)` |
| D7 | Стандартная ошибка | `=D6/КОРЕНЬ(D4)` |
| | | |
| D9 | t-статистика | `=(D5-D3)/D7` |
| D10 | Степени свободы | `=D4-1` |
| D11 | p-value (двухсторонний) | `=2*(1-СТЬЮДРАСП(ABS(D9);D10;ИСТИНА))` |
| D12 | p-value (правосторонний) | `=1-СТЬЮДРАСП(D9;D10;ИСТИНА)` |
| D13 | p-value (левосторонний) | `=СТЬЮДРАСП(D9;D10;ИСТИНА)` |

### Критические значения
```excel
D15: =СТЬЮДРАСПОБР(0,05;D10)  # Для двухстороннего теста
D16: =СТЬЮДРАСПОБР(0,1;D10)   # Для одностороннего теста
```

### Решение гипотезы
```excel
D18: =ЕСЛИ(D11<0.05;"Отвергаем H₀";"Не можем отвергнуть H₀")
```

---

## 🔄 Лист 5: Расчет размера выборки

### Размер выборки для заданной точности ДИ

| Ячейка | Показатель | Формула | Пример |
|--------|------------|---------|--------|
| D3 | Желаемая погрешность (E) | Вводим вручную | 50 |
| D4 | Уровень доверия | `0,95` | 95% |
| D5 | Предварительная оценка σ | Вводим вручную | 400 |
| D6 | z-критическое | `=НОРМСТОБР(1-(1-D4)/2)` | 1.96 |
| D7 | Необходимый размер выборки | `=(D6*D5/D3)^2` | 246 |
| D8 | Размер выборки (округл. вверх) | `=ОКРУГЛВВЕРХ(D7;0)` | 246 |

### Размер выборки для пропорции
```excel
D11: 0,05  # Ожидаемая пропорция p
D12: 0,02  # Желаемая погрешность
D13: =(1,96^2*D11*(1-D11))/D12^2  # Размер выборки
D14: =ОКРУГЛВВЕРХ(D13;0)  # Округленный размер
```

### Консервативная оценка (p = 0.5)
```excel
D16: =(1,96^2*0,25)/D12^2  # Максимальный размер выборки
```

---

## 🔍 Лист 6: Множественные сравнения

### Поправка Бонферрони

| Ячейка | Показатель | Формула |
|--------|------------|---------|
| D3 | Количество тестов (k) | Вводим вручную |
| D4 | Исходный уровень α | `0,05` |
| D5 | Скорректированный α | `=D4/D3` |
| D6 | Критическое p-value | `=D5` |

### Таблица p-values
```excel
# Столбцы F-G: список тестов и их p-values
F8: Тест 1    G8: =вводим p-value
F9: Тест 2    G9: =вводим p-value
...

# Столбец H: значимость после поправки
H8: =ЕСЛИ(G8<$D$5;"Значимо";"Не значимо")
```

### Подсчет значимых результатов
```excel
D10: =СЧЁТЕСЛИ(H:H;"Значимо")  # Количество значимых тестов
D11: =СЧЁТ(G:G)-1  # Общее количество тестов (минус заголовок)
D12: =D10/D11  # Доля значимых тестов
```

---

## 📊 Лист 7: Сводная таблица результатов

### Автоматическая сводка всех расчетов

```excel
A1: СВОДКА СТАТИСТИЧЕСКОГО АНАЛИЗА
A3: ===================================

A5: 1. ДОВЕРИТЕЛЬНЫЙ ИНТЕРВАЛ ДЛЯ СРЕДНЕГО
A6: =СЦЕПИТЬ("Среднее: ";ОКРУГЛ('Лист1'!D4;2);"₽")
A7: =СЦЕПИТЬ("95% ДИ: [";ОКРУГЛ('Лист1'!D14;2);"₽; ";ОКРУГЛ('Лист1'!D15;2);"₽]")
A8: =СЦЕПИТЬ("Ширина ДИ: ";ОКРУГЛ('Лист1'!D16;2);"₽")

A10: 2. ДОВЕРИТЕЛЬНЫЙ ИНТЕРВАЛ ДЛЯ ПРОПОРЦИИ
A11: =СЦЕПИТЬ("Пропорция: ";ОКРУГЛ('Лист2'!D6;2);"%")
A12: =СЦЕПИТЬ("95% ДИ: [";ОКРУГЛ('Лист2'!D14;2);"%; ";ОКРУГЛ('Лист2'!D15;2);"%]")

A14: 3. ДВУХВЫБОРОЧНЫЙ T-ТЕСТ
A15: =СЦЕПИТЬ("Разность средних: ";ОКРУГЛ('Лист3'!D9;2))
A16: =СЦЕПИТЬ("t-статистика: ";ОКРУГЛ('Лист3'!D12;3))
A17: =СЦЕПИТЬ("p-value: ";ОКРУГЛ('Лист3'!D14;4))
A18: ='Лист3'!D23

A20: 4. ОДНОВЫБОРОЧНЫЙ T-ТЕСТ
A21: =СЦЕПИТЬ("Тестируемое значение μ₀: ";'Лист4'!D3)
A22: =СЦЕПИТЬ("Выборочное среднее: ";ОКРУГЛ('Лист4'!D5;2))
A23: =СЦЕПИТЬ("t-статистика: ";ОКРУГЛ('Лист4'!D9;3))
A24: =СЦЕПИТЬ("p-value: ";ОКРУГЛ('Лист4'!D11;4))
A25: ='Лист4'!D18
```

---

## ⚙️ Полезные дополнительные формулы

### Размер эффекта (Cohen's d)
```excel
=ABS(СРЗНАЧ(A:A)-СРЗНАЧ(B:B))/КОРЕНЬ(((СЧЁТЗ(A:A)-1)*СТАНДОТКЛОН.В(A:A)^2+(СЧЁТЗ(B:B)-1)*СТАНДОТКЛОН.В(B:B)^2)/(СЧЁТЗ(A:A)+СЧЁТЗ(B:B)-2))
```

### Мощность теста (приблизительно)
```excel
=1-НОРМРАСП(1,96-ABS(разность_средних)/стандартная_ошибка_разности;0;1;ИСТИНА)
```

### Проверка нормальности (тест Шапиро-Уилка недоступен, используем правило)
```excel
=ЕСЛИ(И(АСИММЕТРИЯ(A:A)<1; ЭКСЦЕСС(A:A)<3; ЭКСЦЕСС(A:A)>-3);"Приближается к нормальному";"Может отличаться от нормального")
```

---

## 🎯 Инструкции по использованию

1. **Скопируйте данные** в столбцы A и B соответствующих листов
2. **Проверьте формулы** - они должны автоматически пересчитаться
3. **Интерпретируйте результаты** используя готовые текстовые формулы
4. **Сохраните файл** с вашими данными для дальнейшего анализа

### Типичные ошибки:
- ❌ Неправильный разделитель в числах (точка вместо запятой)
- ❌ Смешивание текстовых и числовых данных в одном столбце
- ❌ Малый размер выборки для нормального приближения

### Проверки качества данных:
- ✅ Все значения числовые
- ✅ Нет пропусков в ключевых столбцах
- ✅ Размер выборки достаточен (n ≥ 30 для ДИ среднего)
- ✅ Для пропорций: np ≥ 5 и n(1-p) ≥ 5